[flake8]
max-line-length = 120
extend-ignore = E203, W503
exclude = .venv, venv, .venv-pdf, .venv-*, build, dist, .git, __pycache__
per-file-ignores =
	backend/main.py: E402

[tool:pytest]
addopts = -q
