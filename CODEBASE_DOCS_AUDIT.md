# 📘 Codebase Docs Health Check

_Last updated: 2025-11-18 (generated by Codex run)_

This note captures the quick “codebase check” requested for the documentation
surface (starting immediately under `/docs`). Each entry flags whether the
artifact looks current, redundant, or stale, plus next actions.

## Root-Level Files (`/docs/*.md`)

| File | Last Commit | Observed State | Action |
| --- | --- | --- | --- |
| `docs/STRUCTURE.md` | 2025-11-17 | Up-to-date nav map but author checklist unchecked (links/tests outstanding). | Finish checklist, cross-link from `TEC_HUB.md`. |
| `docs/GETTING_STARTED.md` | 2025-11-17 | Mirrors root instructions; content overlaps with `/GETTING_STARTED.md`. | Decide on single canonical location and deep-link to avoid drift. |
| `docs/ALPHA_RELEASE_NOTES.md` | 2025-11-17 | Fresh alpha summary describing current modules. | ✅ Nothing urgent; keep as release artifact. |
| `docs/CONSCIOUSNESS_INTEGRATION_ROADMAP.md` | 2025-11-17 | Roadmap references Nov 2025 status; Python agent implementation still at 20%. | Update once Screen A + Python agents mature; mark phase completion when Airth is live. |
| `docs/PLATFORM_ARCHITECTURE.md` | 2025-11-17 | High-level overview, but no diagrams embedded and references pre-wireframe UI. | Add Screen A topology + Tailwind surface callouts. |
| `docs/PYTHON_AGENT_STACK_AUDIT.md` | 2025-11-17 | Documents FastAPI/Typer status; notes missing Airth guard. | Revise after implementing `src/tec_tgcr/agents/*`. |
| `docs/SESSION_COMPLETION_SUMMARY.md` | 2025-11-17 | Session telemetry doc, no open issues spotted. | ✅ Leave as-is. |
| `docs/TECH_FILES_COHERENCE_AUDIT.md` | 2025-11-17 | References pre-Next UI; still valid but lacking Screen A assets. | Add new components + tests to checklist. |
| `docs/index.md` | 2025-11-17 | GitHub Pages landing; not yet referencing Screen A. | Add screenshot + link to `frontend/app/page.tsx`. |
| `docs/SECRETS_MANAGEMENT.md` | staged (modified) | Primary secrets SOP. | Finish edits, then remove redundant backups noted below. |
| `docs/SECRETS_MANAGEMENT.md.backup` | untracked | Duplicate of secrets doc. | Archive inside `docs/archive/` or delete once canonical file committed. |
| `docs/SECRETS_MANAGEMENT.md.prune-backup` | untracked | Partial prune artifact. | Same action as above. |

## Directories Right Under `/docs`

- `docs/api/` — empty placeholder. Either backfill API reference or remove from
  navigation (currently linked from `STRUCTURE.md`).
- `docs/architecture/ADR/` — empty, so ADR process not instantiated. Seed with
  first ADR (e.g., Screen A architecture) or hide folder.
- `docs/architecture/*` — populated but last update before Screen A; add new
  React module descriptions for Resonance Engine UI.
- `docs/brand/` — contains logos/specs; verify compliance with `assets/logo`.
- `docs/consciousness/`, `docs/governance/`, `docs/reference/` — rich and
  current per November 2025 timeline; keep referencing in README + new PR.
- `docs/operations/` — `TEC_HUB.md` still points to placeholder UI; update once
  Screen A shipped.
- `docs/llm-onboarding/` — recently refreshed; no action beyond linking new UI.
- `docs/security/` — contains incident response logs; ensure secrets workflow
  improvements from `.secrets.scan.json` (see below) propagate here.

## Detect-Secrets Scan

- `.venv/bin/detect-secrets scan --all-files` was attempted twice; both runs hit
  the 10-minute ceiling because of repository size (large `node_modules/` and
  design exports).
- Recommended mitigation:
  - Use `detect-secrets scan --all-files --exclude-files '(node_modules|\\.git|\\.venv|venv|\\.next|__pycache__)'`
    inside the `frontend`/`backend` subtrees separately.
  - Commit the generated `.secrets.scan.json` or update `.secrets.baseline`
    after manual review.

## Next Steps

1. Resolve the secrets doc backups (`docs/SECRETS_MANAGEMENT.md.backup` and
   `.prune-backup`).
2. Backfill `docs/api/` + `docs/architecture/ADR/`.
3. Once Screen A ships, refresh `docs/PLATFORM_ARCHITECTURE.md`,
   `docs/index.md`, and `TEC_HUB.md` with component screenshots.
4. Rerun detect-secrets with directory exclusions and capture findings in
   `.secrets.scan.json`.
